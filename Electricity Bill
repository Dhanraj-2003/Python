units = float(input("Enter electricity units consumed: "))
if units <= 100:
    cost = units * 1.5
elif units <= 300:
    cost = 100 * 1.5 + (units - 100) * 2.5
else:
    cost = 100 * 1.5 + 200 * 2.5 + (units - 300) * 3.5
print("Total electricity bill:", cost)
